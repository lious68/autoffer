# 贡献指南

欢迎独立维护一个平台，也欢迎补充题型、模型接口和回归样例。

## 选择贡献入口

| 想贡献的内容        | 主要修改位置                                | 验证依据                                               |
| ------------------- | ------------------------------------------- | ------------------------------------------------------ |
| 新平台或新布局      | `platforms/<id>/`、registry                 | 脱敏 fixture + `assertPlatformContract` + 实际字段断言 |
| 自动选答与翻页      | 平台 `actions.ts` 与注册能力                | 控件选中回读、取消、旧题、遮挡、末题停止               |
| 专业/行测等领域策略 | `solvers/policy.ts`                         | 分类组合与模型请求测试                                 |
| 新题目形式          | classification、Provider 输出校验、平台动作 | 题目形式 × 领域 × 模态的回归                           |
| 新模型接口          | `providers/`、渠道配置/权限                 | 固定成功/失败响应、Key 隔离                            |

先读 [题目模型](docs/question-model.md) 和 [架构评估](docs/architecture-review.md)。新平台可以只提供识题能力，自动模式会生成建议后等待手动作答，不要求贡献者一次实现整个流程。

## 开发流程

1. 使用 Node.js 22.18 或更高版本，运行 `npm ci`。
2. 新平台用 `npm run platform:new -- platform-id "平台名称"` 生成基础文件。
3. 先加入获得授权且脱敏的 HTML fixture，再调整匹配规则和解析代码。
4. 用测试确认题干、选项、材料、题型、图片警告和不匹配场景。
5. `npm run format` 后运行 `npm run check`。
6. 按 [手动验收](docs/testing.md) 在 Chrome 中验证，PR 中明确哪些验证没有完成。

## 修改边界

- `platforms/` 的提取器只读页面并返回统一 `Question`；可选 `actions.ts` 负责选答、翻页与回读验证，由 `index.ts` 组合注册。不访问 API、不处理 Key。
- `solvers/` 只处理分类、求解模式和领域提示，不访问 DOM、网络或 Chrome API。
- `content/` 通过通用平台 runtime 调用动作，不直接导入具体平台。
- `providers/` 只处理题目与模型响应，不引用 DOM、Chrome API 或平台选择器。
- `background/` 负责消息边界、权限、Key 和请求生命周期。
- `ui/` 只通过 `Client` 交互；网页和模型内容必须使用 `textContent` 渲染。
- 不加入远程执行代码、动态下载模板或包含敏感页面数据的日志。
- 新依赖请说明用途；新增模型接口要附固定模拟响应及错误响应测试。

## 平台状态

- `demo`：项目自编样例，不代表真实平台。
- `experimental`：正在适配，fixture 和基础测试通过，真实环境验证尚不完整。
- `verified`：在平台说明中记录验证日期、页面版本、题型、浏览器版本及限制；不是永久兼容保证。

fixture 必须去除姓名、手机号、身份证、简历、账号、Cookie、token、考试邀请码、个人答案和供应商未授权内容。图片使用自制替代图；不提交生产题库。

修复一个平台时，尽量只改其目录及相关 fixture/test。通用引擎变更必须运行所有平台测试，防止影响其他维护者。

## PR 内容

描述用户可见的问题及结果；列出平台支持范围、fixture 来源、验证方法和已知限制。不要用“全平台通用”“完全准确”替代实测证据。
